import * as React from 'react';
import Layout from 'containers/layout/Layout';
import { Row, Col } from 'antd';

const Root: React.FC = () => {
  return (
    <Layout span={24}>
      Test <h1>Test</h1>
    </Layout>
  );
};

export default Root;
